# Matches the credentials in docker-compose.yml.
# Host port is 5433 so it does not clash with a local PostgreSQL install.
DATABASE_URL="postgresql://inventory:inventory@localhost:5433/inventory?schema=public"
